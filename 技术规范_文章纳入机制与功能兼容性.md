# 外刊阅读器 — 文章纳入机制与功能兼容性技术规范

> **文档目的**
>
> ：定义新文章纳入机制和后续功能开发的兼容性规则，确保新增文章时已有功能不出现 bug，后续功能遵循统一规范。
> **执行对象**
>
> ：其他 agent 按本规范逐步实施。
> **项目路径**
>
> ：
>
> `E:\project\外刊阅读器`



***

## 一、当前问题诊断

### 1.1 硬编码问题（P0，必须首先解决）

**位置**：`src/js/10-toolbar.js` 第 2-12 行



```
const EXAM_SLUGS = {
  'SundayTimes_2026-06-14_Hidden_Cost_AI_Fortson_EN-CN_final.html': 'ai_cost',
  'WSJ_2026-03-21_AI_Regulation_Fryer_EN-CN_final.html': 'ai_regulation',
  // ... 共9篇硬编码
};
```

**问题**：



* 每加一篇文章必须手动修改此映射表 → 重新合并 reader.js → 重新部署

* 文章文件名是完整字符串，任何命名差异（大小写、空格、后缀）都会导致匹配失败

* 工具栏的 "考试" 菜单依赖此映射判断是否显示，匹配失败则考试功能静默消失

**影响范围**：工具栏考试菜单、考试模式入口、题库数据关联。

### 1.2 数据结构问题



| 数据 key                        | 存储方式 | 问题                    |
| ----------------------------- | ---- | --------------------- |
| `annotations:<articleId>`     | 按篇独立 | ✅ 无问题                 |
| `summary:<articleId>`         | 按篇独立 | ✅ 无问题                 |
| `translation:<articleId>`     | 按篇独立 | ✅ 无问题                 |
| `settings:<articleId>`        | 按篇独立 | ✅ 无问题                 |
| `syntax:<articleId>`          | 按篇独立 | ✅ 无问题                 |
| `wsj_writing:materials`       | 全局共享 | ⚠️ 无来源标记，无法区分哪篇文章的素材  |
| `wsj_writing:advice`          | 全局共享 | ⚠️ 有 articleId 字段但未强制 |
| `wsj_writing:notes`           | 全局共享 | ⚠️ 无来源标记              |
| `wsj_roots:cards`             | 全局共享 | ⚠️ 无来源标记              |
| 生词本（annotations bucket=vocab） | 按篇独立 | ⚠️ 跨文章复习时无法聚合         |

### 1.3 构建流程问题



* `scripts/build_html.py` 依赖 `_articles.json`，新文章需要手动准备 JSON

* 文章 HTML 中引用的 `reader.js`/`reader.css`/`exam-panel.js` 是相对路径，新文章放在子目录会失效

* 缓存版本号 `?v=xxx` 需要手动更新所有文章



***

## 二、第一阶段：文章纳入机制（P0，优先实施）

### 2.1 文章元数据驱动（核心方案）

**目标**：消除 EXAM\_SLUGS 硬编码，改为从文章 HTML 的 `<meta>` 标签读取元数据。

#### 2.1.1 每篇文章 HTML 必须包含的 meta 标签

在 `<head>` 中添加：



```
<meta name="article-id" content="ai_cost">
<meta name="article-title" content="美国巨头在AI上烧掉数十亿美元">
<meta name="article-source" content="Sunday Times">
<meta name="article-date" content="2026-06-14">
<meta name="article-topic" content="AI,经济,科技">
<meta name="article-has-exam" content="true">
<meta name="article-exam-types" content="reading,cloze,newtype,translation">
```

**字段说明**：



| 字段                   | 必填 | 说明                                           |
| -------------------- | -- | -------------------------------------------- |
| `article-id`         | ✅  | 文章唯一标识，用于 localStorage key 和题库关联，用小写英文 + 下划线 |
| `article-title`      | ✅  | 中文标题，用于 Hub 页面和学习总览                          |
| `article-source`     | ✅  | 来源刊物（WSJ / Science / SundayTimes 等）          |
| `article-date`       | ✅  | 文章日期 YYYY-MM-DD                              |
| `article-topic`      | 推荐 | 主题标签，逗号分隔，用于跨文章聚类                            |
| `article-has-exam`   | ✅  | 是否有考试题目，"true"/"false"                       |
| `article-exam-types` | 可选 | 支持的考试题型，逗号分隔                                 |

#### 2.1.2 reader.js 读取 meta 的实现

在 `00-head.js` 中添加文章元数据读取函数：



```
// 文章元数据（从 <meta> 标签读取，替代硬编码 EXAM_SLUGS）
const articleMeta = (function() {
  function getMeta(name) {
    const el = document.querySelector('meta[name="' + name + '"]');
    return el ? el.getAttribute('content') : '';
  }
  return {
    id: getMeta('article-id') || location.pathname.split('/').pop().replace(/\.html?$/, ''),
    title: getMeta('article-title') || '',
    source: getMeta('article-source') || '',
    date: getMeta('article-date') || '',
    topic: (getMeta('article-topic') || '').split(',').map(s => s.trim()).filter(Boolean),
    hasExam: getMeta('article-has-exam') === 'true',
    examTypes: (getMeta('article-exam-types') || '').split(',').map(s => s.trim()).filter(Boolean)
  };
})();
```

**注意**：`articleId` 变量（用于 localStorage key）应改为从 `articleMeta.id` 取值，而不是从 URL 文件名取。这样即使文件名变化，数据也不会丢失。

#### 2.1.3 移除 EXAM\_SLUGS 硬编码

在 `10-toolbar.js` 的 `buildMenus()` 中：

**修改前**：



```
const examSlug = EXAM_SLUGS[decodeURIComponent(location.pathname.split('/').pop() || '')];
if (examSlug) { /* 显示考试菜单 */ }
```

**修改后**：



```
if (articleMeta.hasExam) { /* 显示考试菜单 */ }
```

删除整个 `EXAM_SLUGS` 常量定义。

### 2.2 文章自动发现机制（Hub 页面）

**目标**：Hub 页面不再硬编码文章列表，改为自动扫描。

#### 2.2.1 文章清单文件

在 `articles/` 目录下创建 `manifest.json`：



```
{
  "version": 1,
  "articles": [
    {
      "id": "ai_cost",
      "file": "SundayTimes_2026-06-14_Hidden_Cost_AI_Fortson_EN-CN_final.html",
      "title": "美国巨头在AI上烧掉数十亿美元",
      "source": "Sunday Times",
      "date": "2026-06-14",
      "topic": ["AI", "经济", "科技"],
      "hasExam": true
    }
  ]
}
```

#### 2.2.2 Hub 页面读取 manifest

`WSJ_Hub.html` 改为 fetch `manifest.json` 动态渲染文章列表，而不是硬编码链接。

**降级方案**：如果 fetch 失败（file:// 协议下可能被浏览器拦截），回退到硬编码列表 + 提示用户。

#### 2.2.3 新文章纳入流程（标准化）

新增一篇文章只需 3 步：



1. 将文章 HTML 放入 `articles/` 目录，确保 `<head>` 中有完整的 meta 标签

2. 在 `articles/manifest.json` 的 `articles` 数组中添加一条记录

3. （可选）如果有考试题目，确保考试数据与 `article-id` 关联

**不需要**：修改 reader.js、修改 EXAM\_SLUGS、重新合并代码（除非 reader.js 本身有更新）。

### 2.3 构建脚本增强

修改 `scripts/build_html.py`：



1. 构建时自动扫描 `articles/` 目录下的 HTML 文件

2. 从每个 HTML 中提取 meta 标签

3. 自动生成 / 更新 `manifest.json`

4. 自动为所有文章更新缓存版本号

5. 校验每篇文章的 meta 标签完整性，缺失时给出警告



***

## 三、第二阶段：数据结构规范（所有功能必须遵守）

### 3.1 核心原则

> **所有用户产生的数据，必须能追溯到来源文章。**
> **所有全局共享数据，必须带 articleId 字段。**
> **所有新功能的 localStorage key，必须遵循命名规范。**

### 3.2 localStorage key 命名规范



```
<模块>:<类型>:<articleId|global>
```



| 模块     | 示例 key                    | 说明                 |
| ------ | ------------------------- | ------------------ |
| 阅读标注   | `annotations:<articleId>` | 按篇独立               |
| 段落概要   | `summary:<articleId>`     | 按篇独立               |
| 翻译编辑   | `translation:<articleId>` | 按篇独立               |
| 阅读设置   | `settings:<articleId>`    | 按篇独立               |
| 长难句    | `syntax:<articleId>`      | 按篇独立               |
| 写作素材   | `writing:materials`       | 全局，每条记录带 articleId |
| 建议文    | `writing:advice`          | 全局，每条记录带 articleId |
| 写作笔记   | `writing:notes`           | 全局，每条记录带 articleId |
| 词根卡片   | `roots:cards`             | 全局，每条记录带 articleId |
| 同义词    | `writing:synonyms`        | 全局，每条记录带 articleId |
| 考试错题   | `exam:wrong:<articleId>`  | 按篇独立               |
| 考试成绩   | `exam:stats:<articleId>`  | 按篇独立               |
| 全局词汇索引 | `vocab:index`             | 全局，按词聚合所有文章        |
| 应用设置   | `app:settings`            | 全局应用级设置            |

**废弃旧 key**：`wsj_writing:materials` → `writing:materials`，需要数据迁移（见 3.5）。

### 3.3 全局数据记录的必填字段

所有存入全局 key（`writing:materials`、`writing:advice`、`writing:notes`、`roots:cards`、`writing:synonyms`）的记录，必须包含：



```
{
  "id": "唯一ID",
  "articleId": "来源文章ID（如 ai_cost），全局手动创建的用 'manual'",
  "articleTitle": "来源文章标题（冗余存储，避免跨页面查询）",
  "createdAt": "ISO时间戳",
  "updatedAt": "ISO时间戳",
  // ... 业务字段
}
```

**为什么要冗余存储 articleTitle**：写作工坊展示素材列表时，如果只有 articleId，需要查询 manifest 才能拿到标题，增加复杂度。冗余存储标题字段，展示时直接用。

### 3.4 按篇数据与全局数据的转换规则



* **按篇数据**（annotations/summary/translation/syntax/exam:wrong）：天然隔离，新文章加入不影响已有数据

* **全局数据**（writing:materials/roots:cards 等）：新文章产生的数据追加到数组末尾，不影响已有记录

* **聚合数据**（vocab:index）：新文章加入时增量追加该篇的词汇条目，不全量重建

### 3.5 数据迁移机制

每次 reader.js 版本升级时，在 `00-head.js` 的 `loadAll()` 中执行迁移检查：



```
function migrateData() {
  const MIGRATION_KEY = 'app:migration_version';
  const currentVersion = parseInt(localStorage.getItem(MIGRATION_KEY) || '0', 10);
  
  if (currentVersion < 1) {
    // v1: wsj_writing:materials → writing:materials，添加 articleId
    try {
      const old = localStorage.getItem('wsj_writing:materials');
      if (old) {
        const arr = JSON.parse(old);
        arr.forEach(item => {
          if (!item.articleId) item.articleId = articleMeta.id;
          if (!item.articleTitle) item.articleTitle = articleMeta.title;
        });
        localStorage.setItem('writing:materials', JSON.stringify(arr));
        localStorage.removeItem('wsj_writing:materials');
      }
    } catch(e) {}
    // 同理迁移 wsj_writing:advice → writing:advice, wsj_writing:notes → writing:notes, wsj_roots:cards → roots:cards
    localStorage.setItem(MIGRATION_KEY, '1');
  }
  
  if (currentVersion < 2) {
    // v2: 未来的迁移逻辑
    localStorage.setItem(MIGRATION_KEY, '2');
  }
}
```

**规则**：



* 迁移函数必须幂等（重复执行不产生错误数据）

* 每次迁移只升一个版本号，逐步迁移

* 迁移失败时静默降级，不阻塞页面加载

* 迁移前不删除旧 key，迁移成功后才删除



***

## 四、第三阶段：功能开发兼容性规则

### 4.1 新功能开发检查清单

每个新功能开发前，必须确认以下事项：

#### 4.1.1 数据层



* [ ] localStorage key 遵循 `模块:类型:范围` 命名规范

* [ ] 全局数据每条记录带 `articleId` 和 `articleTitle`

* [ ] 按篇数据使用 `articleMeta.id`（不是 URL 文件名）作为 key 后缀

* [ ] 数据结构中有 `createdAt`/`updatedAt` 时间戳

* [ ] 考虑了新文章加入时的数据兼容性

#### 4.1.2 UI 层



* [ ] 工具栏菜单项的显示 / 隐藏基于 `articleMeta.hasExam` 或功能特性标记，不基于硬编码文章列表

* [ ] 面板 / 弹窗使用统一的 `.syntax-panel` 基类 + `visible` 类控制显示（不要单独设置 `display:flex`）

* [ ] 所有关闭按钮用 `querySelectorAll('[data-close]')` 绑定（不要只绑第一个）

* [ ] 新 Tab / 新面板有 "空状态" 提示（无数据时显示引导文案）

#### 4.1.3 构建层



* [ ] 如果修改了 `src/js/` 下的文件，必须运行 `python scripts/concat_reader.py` 重新合并

* [ ] 合并后运行 `node -c src/reader.js` 语法检查

* [ ] 部署到 `articles/` 目录后更新所有文章的缓存版本号

* [ ] 新功能的 CSS 加在 `src/reader.css` 末尾，不覆盖已有样式

#### 4.1.4 兼容性



* [ ] 不修改已有 localStorage key 的名称（如需改名，走数据迁移）

* [ ] 不修改已有数据结构的字段名（如需改，添加新字段，旧字段保留兼容）

* [ ] 新功能在没有对应数据时优雅降级（不报错、不白屏）

* [ ] 考试相关功能在 `articleMeta.hasExam=false` 的文章中不显示

### 4.2 面板开发规范（统一弹窗模式）

所有新增弹窗 / 面板必须遵循以下模式：



```
function openXxxPanel() {
  let panel = document.getElementById('xxx-panel');
  if (!panel) {
    panel = document.createElement('div');
    panel.id = 'xxx-panel';
    panel.className = 'syntax-panel xxx-panel';  // 必须包含 syntax-panel 基类
    panel.innerHTML =
      '<div class="syntax-header"><h3>标题</h3><button type="button" data-close="1">✕</button></div>' +
      '<div class="xxx-body">内容</div>' +
      '<div class="syntax-actions"><button type="button" data-close="1">关闭</button></div>';
    document.body.appendChild(panel);
    // 绑定所有 data-close 按钮
    panel.querySelectorAll('[data-close]').forEach(btn => {
      btn.addEventListener('click', () => panel.classList.remove('visible'));
    });
  }
  panel.classList.add('visible');
}
```

**CSS 规范**：



```
.xxx-panel { width: min(92vw, 600px); max-height: 80vh; flex-direction: column; }
/* 不要设置 display:flex！由 .syntax-panel.visible 控制 */
```

### 4.3 工具栏菜单开发规范

新增工具栏菜单项时：



1. 在 `buildMenus()` 中对应的 menu 字符串里添加 `menuItemHTML()` 调用

2. 菜单按钮的显示条件基于 `articleMeta` 或功能开关，不基于文章文件名

3. 按钮点击事件在 `DOMContentLoaded` 中通过 `document.getElementById()` 绑定

4. 如果按钮依赖按需加载的 exam-panel.js，用 `loadExamPanel().then()` 包裹

### 4.4 考试功能开发规范



* 考试题目数据与 `articleMeta.id` 关联，不与文件名关联

* 考试记录按篇存储：`exam:stats:<articleId>`、`exam:wrong:<articleId>`

* 在没有考试题目的文章中（`articleMeta.hasExam=false`），考试菜单和入口不显示

* 新题型添加时，在 `articleMeta.examTypes` 中声明，工具栏据此显示对应入口



***

## 五、第四阶段：容量管理与性能保障

### 5.1 localStorage 容量预警（已有功能，需增强）

当前已有 `checkStorageQuota()`，需增强：



1. 按数据类型统计占用（标注 / 概要 / 翻译 / 写作 / 考试 / 全局索引）

2. 超过 70% 时显示温和提示，超过 90% 时显示强制提示

3. 提供 "一键清理可重建数据" 功能（概要 / 翻译可从 HTML 重新加载，标注 / 写作素材不可重建）

### 5.2 数据分级清理策略



| 数据类型                      | 可重建？                | 清理优先级 |
| ------------------------- | ------------------- | ----- |
| `summary:<articleId>`     | ✅ 可从 HTML 重新加载      | 最高    |
| `translation:<articleId>` | ✅ 可从 HTML 重新加载      | 最高    |
| `settings:<articleId>`    | ✅ 有默认值              | 中     |
| `annotations:<articleId>` | ❌ 用户创造              | 最低    |
| `syntax:<articleId>`      | ❌ 用户创造              | 最低    |
| `writing:*`               | ❌ 用户创造              | 最低    |
| `exam:wrong:*`            | ❌ 用户创造              | 最低    |
| `vocab:index`             | ✅ 可从 annotations 重建 | 高     |

### 5.3 新文章加入时的性能保障



* 全局词汇索引（`vocab:index`）采用增量追加，不全量重建

* 新文章的生词 / 错题不立即进入间隔重复队列，按每天最多 N 条的速率逐步释放

* Hub 页面的文章列表懒加载，不一次性渲染所有文章的详细数据



***

## 六、实施步骤与验收标准

### 步骤 1：文章元数据驱动（P0）

**实施内容**：



1. 为现有 9 篇文章 HTML 添加 meta 标签（article-id/title/source/date/topic/has-exam/exam-types）

2. 在 `00-head.js` 中添加 `articleMeta` 读取逻辑

3. 将 `articleId` 变量改为从 `articleMeta.id` 取值

4. 删除 `10-toolbar.js` 中的 `EXAM_SLUGS` 硬编码

5. `buildMenus()` 中考试菜单判断改为 `articleMeta.hasExam`

**验收标准**：



* [ ] 9 篇文章的工具栏考试菜单正常显示

* [ ] 复制一篇文章 HTML，改文件名但不改 meta，打开后考试菜单正常

* [ ] 复制一篇文章 HTML，去掉 `article-has-exam` meta，打开后考试菜单不显示

* [ ] 已有标注 / 概要 / 翻译数据不丢失（因为 articleId 不变）

### 步骤 2：文章清单与自动发现（P0）

**实施内容**：



1. 创建 `articles/manifest.json`，包含 9 篇文章信息

2. 修改 `WSJ_Hub.html` 从 manifest 读取文章列表

3. 修改 `build_html.py` 自动扫描 HTML 生成 manifest

**验收标准**：



* [ ] Hub 页面正常显示 9 篇文章

* [ ] 在 manifest 中添加一篇不存在的文章记录，Hub 显示但点击 404（合理降级）

* [ ] 在 articles/ 目录放一篇带 meta 的新 HTML，运行构建脚本后 manifest 自动更新

### 步骤 3：数据结构规范化（P1）

**实施内容**：



1. 全局数据（writing:materials/advice/notes/roots:cards）每条记录添加 articleId + articleTitle

2. 编写数据迁移函数 `migrateData()`，从旧 key 迁移到新 key 并补全字段

3. 写作工坊的素材 / 建议文 / 词根列表显示来源文章标签

**验收标准**：



* [ ] 旧用户打开页面后，已有素材自动迁移到新 key，不丢失

* [ ] 新存的素材带 articleId 和 articleTitle

* [ ] 写作工坊 "全部"Tab 中每条记录显示来源文章标签

* [ ] 清除 localStorage 后重新使用，数据结构符合规范

### 步骤 4：功能开发规则文档化（P1）

**实施内容**：



1. 将本规范的 4.1-4.4 节作为开发检查清单

2. 在 `scripts/` 下添加 `validate_article.py` 脚本，校验文章 meta 完整性和数据结构规范

**验收标准**：



* [ ] 运行校验脚本能检测出缺失 meta 的文章

* [ ] 后续新功能开发遵循检查清单



***

## 七、关键文件清单



| 文件                            | 作用   | 修改内容                                    |
| ----------------------------- | ---- | --------------------------------------- |
| `src/js/00-head.js`           | 公共头部 | 添加 articleMeta、migrateData、localDateStr |
| `src/js/10-toolbar.js`        | 工具栏  | 删除 EXAM\_SLUGS，改用 articleMeta.hasExam   |
| `articles/*.html`（9 篇）        | 文章页面 | 添加 meta 标签                              |
| `articles/manifest.json`      | 文章清单 | 新建                                      |
| `articles/WSJ_Hub.html`       | 文章中心 | 从 manifest 读取列表                         |
| `scripts/build_html.py`       | 构建脚本 | 自动生成 manifest、更新缓存版本                    |
| `scripts/validate_article.py` | 校验脚本 | 新建，校验 meta 完整性                          |
| `scripts/concat_reader.py`    | 合并脚本 | 无需修改                                    |



***

## 八、风险与回退



| 风险                                            | 影响 | 回退方案                        |
| --------------------------------------------- | -- | --------------------------- |
| meta 标签读取失败（file:// 协议下 querySelector 正常，无风险） | 低  | 回退到 URL 文件名作为 articleId     |
| 数据迁移失败导致旧数据丢失                                 | 高  | 迁移前不删除旧 key，迁移成功后才删除；迁移函数幂等 |
| manifest.json 在 file:// 下 fetch 被拦截           | 中  | Hub 页面回退到硬编码列表              |
| 新文章 meta 不完整导致功能异常                            | 中  | 校验脚本在构建时报警告；运行时缺失字段用默认值     |



***

*文档版本：v1.0 | 生成日期：2026-09-24*